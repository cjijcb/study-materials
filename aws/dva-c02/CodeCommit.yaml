CodeCommit:
  - code repository like GitHub and GitLab
  - use for version control of application
  Authentication:
    - SSH keys
    - HTTPS
  Authorization:
    - IAM
  Encryption:
    At Rest:
      - AWS KMS
    In-Transit:
      - SSH
      - HTTPS
  Cross-Account Access:
    - via IAM role + IAM STS
  Notification:
    - AWS Lamdba or AWS SNS for repository events like push, delete, and create.
